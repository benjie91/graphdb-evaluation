pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'graphdb'

include('graphdb-evaluation') // For evaluation against the graphDB (e.g. Arango, Neo4j, TigerGraph)
include('graphdb-frontend')   // For frontend visualisation of graph node and edges
